Iola NLE

- Create sub-channels for debug messages

- Display mark in/out in the monitors

- Implement more keyboard shortcuts for editing and navigation
  - Lift (z)
  - Extract (x)
  - Copy (c)

- Add fade transition

- Load clip in source monitor using URI from clipboard (ctrl+v)

- Copy URI of clip in source monitor to clipboard (ctrl+c)

- Implement an OpenGL consumer

- Implement the timeline

- "Open"/"Save" serialization of the project

- Implement a dialog for editing the profile (project setting)

- Improve the bin

- Export AVI

- Export EDL

- Refactor into proper MVC design pattern

- Add unit tests to project and enable testing in CMake

- Add pre-commit hook in git to run unit tests

- Add Doxygen and document code

- Implement the command pattern for editing to enable undo/redo

- Store the path to a clip in a 'path' property

- Use syslog for logging

- Add a statusbar to the main window and show contextual help
  messages.

- Create pixmaps for buttons
